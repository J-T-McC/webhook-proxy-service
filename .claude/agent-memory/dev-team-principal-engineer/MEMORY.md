- [Project structure](project_structure.md) — ADRs in docs/architecture/ (001–026); ingest→dispatch seam + **must-precede-the-2xx work belongs pre-dispatch, never in the pipeline**; payload-table LONGBLOB/MySQL-only (**whole migration set unrunnable on SQLite — `sail test` only**); mode/single-resolver seams; **short-circuiting step strands the dispatch**; **retry re-sends stored dispatched bytes, replay re-runs the pipeline**; retention hold set H0–H5; `ApplyTeamScope` skips `Delivery`+`WebhookEvent`; `ingest_id` is the only join reaching every attempt; **FIFO claim-lease seam**; **queue-payload seam** (`ShouldBeEncrypted` a no-op under laravel-actions, Horizon a second 7-day payload store); **secret-leak seams** (`QueryException` interpolates bindings, `Arr::forget()` has no wildcard, three unordered retention literals); **encode intent as a positive value, never an absence**; **ingest raw-body seam** (route outside the web group, multipart makes `getContent()` empty, `trustProxies(at:'*')` makes `fullUrl()` caller-controllable); **outbound header seam + the #10 secret machinery — `SecretStore` is purpose-parameterised with no verification branch, `StandardWebhooks::verify()` is the outbound signing suites' receiver-side oracle, `ProxySecret`'s `purpose` enum cast couples an enum case to its rows, and forwarding `authorization` makes the AC30 credential collision the ordinary case**
- [Plan & ADR house format](plan_and_adr_house_format.md) — house deviations from the plugin templates; the Owner gate carve-out from PE self-certification (no-flags, flags-outstanding, and flags-but-no-ADR shapes); `## Revision A`/`B` on plans and on Proposed and Accepted ADRs; the `## Pointer` a certified plan takes while an ADR is only Proposed; defect-driven ADRs with no PRD; partial supersession of Accepted ADRs with a named+quoted Positions-superseded table and a "not superseded though it looks it" list; correcting wrong *premises* rather than conclusions; `git diff --numstat` zero-deletions as the checkable form of "additive pointer"; the feasibility study as a fourth PE artifact; **full supersession of an Accepted ADR on a capability withdrawal — keep the file, carry forward the one passage a surviving ADR cites, rule what stays at the level of the method, and rule the migration on "rows are not schema"**
