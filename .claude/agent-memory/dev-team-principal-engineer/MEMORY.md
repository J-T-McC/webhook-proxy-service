- [Project structure](project_structure.md) — the traps a ruling must account for: MySQL-only migration set (`sail test` only), `json` columns can never take an `encrypted*` cast, must-precede-the-2xx work belongs pre-dispatch, a short-circuiting step strands the dispatch, retry re-sends stored bytes while replay re-runs the pipeline, retention hold and FIFO claim-lease seams, `ShouldBeEncrypted` a no-op under laravel-actions, secret-leak seams (QueryException bindings, `Arr::forget()` has no wildcard), encode intent as a positive value never an absence, ingest raw-body seam, post-ADR-026 outbound header and secret machinery, `ApplyTeamScope` skips `Delivery`+`WebhookEvent`
- [Plan & ADR house format](plan_and_adr_house_format.md) — house deviations from the plugin templates: the three Owner-gate/certification shapes, Revision A/B mechanics on plans and ADRs, the `## Pointer` for a still-Proposed ADR, partial and full supersession (named+quoted Positions-superseded table, "not superseded though it looks it" list, premise correction, `git diff --numstat` zero deletions, rows-are-not-schema), the rules for handling Owner rulings, and the defect-driven ADR and feasibility-study shapes
