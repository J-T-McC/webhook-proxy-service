- [Project structure](project_structure.md) — ADRs in docs/architecture/ (001–019); ingest→dispatch seam + **must-precede-the-2xx work belongs pre-dispatch, never in the pipeline**; payload-table LONGBLOB/MySQL-only (**whole migration set unrunnable on SQLite — `sail test` only**) + json-can't-be-encrypted + column-type-choice gotchas; mode/single-resolver seams (+ standing breaches), ProxyResource-serves-three-surfaces and ProxyForm mount-seed traps; **short-circuiting step strands the dispatch → FIFO stall + H2 immortalizes**; **retry re-sends stored dispatched bytes, replay re-runs the pipeline**; retention hold set H0–H5 traps; native TeamRole/TeamPermission/HasTeams seam; **ApplyTeamScope skips `Delivery`+`WebhookEvent`**; terminal-row `updated_at` frozen (usable time anchor); `ingest_id` is the only join reaching every attempt; no SSR entrypoint despite `ssr.enabled`; canvasKit is the PR#12 colour-token fix; **`ProxyEventController` filter seam — one `$start`/`$end` build point, but `resolveFilters()`'s short-circuit swallows any new parameter**; **analytics window built three ways (two disagree) + no portable hour-truncating SQL**; **FIFO claim-lease seam — lease never renewed, `WithoutOverlapping` TTL == lease so it guards nothing, `resume()` re-sends a `dispatched` row, `settleOrHold` settle-branch is a blind update, and only `timeout < lease` closes the window**; **queue-payload seam — attempt 1 is the only delivery job carrying bytes, `dispatchedBytesFor()` always resolves via ADR-013's divergence gate, `ShouldBeEncrypted` is a silent no-op under laravel-actions while `SerializesModels` does apply (top-level params only), Horizon is a second 7-day payload store, 50 MiB inbound vs SQS 256 KiB**; **secret-leak seams — `QueryException` interpolates bindings into the message (→ `failed_jobs`, Horizon, log) and only the `encrypted` casts keep it ciphertext; `Arr::forget()` has no wildcard so `dontFlash` can't reach nested keys and `SESSION_DRIVER=database` stores flashed input; no `Cache::` call exists in `app/` at all; three retention literals (prune 168h, Horizon 10080min, `RETENTION_DAYS`) with no test; `PasswordInput.vue` already exists; `Create.vue` gets no proxy resource**
- [Plan & ADR house format](plan_and_adr_house_format.md) — house deviations from the plugin templates; the Owner gate carve-out from PE self-certification, both the no-flags and flags-outstanding shapes; the Owner-amendment `§ Revision A` pattern, mid-design (partial supersession) and post-review (in-place ruling rewrite + re-certification); **flags-but-no-ADR shape** + pulling the design's PE-triggered contingencies; **`Revision A` also covers the PE's own post-certification ruling on a downstream question doc**; **`Revision B` = a later PM amendment, newest section on top, rulings appended, milestone appended, re-certification stacked**; **defect-driven ADR with no PRD/plan — guarantee restatement up front, change set in § Impact, PROPOSED-amendment vs PROPOSED-supersession annotations**; **`Revision A` on a still-Proposed ADR when an Owner requirement answers a gate — quote the requirement verbatim, mark the now-adopted Alternatives bullet `[ADOPTED]` keeping the wrong reasoning visible, say "gate withdrawn, nothing replaces it"**; **`Revision B` on an ADR = acceptance + a new ruling in one edit: keep the flags heading struck through, rewrite PROPOSED annotations to SUPERSEDED/AMENDED, and when the Owner's suggested mechanism works but still loses, say so in those words**; **an Owner ruling landing mid-design that contradicts an approved PRD/design — banner every artifact, build the backend, STOP at the surface, split the milestone, exhaustive question doc to the PM; a directed mechanism is presented as a CHOICE with the alternative fully enumerated and the flags coupled; "storage general, behaviour narrow" as the reusable resolution**
