- [Testing patterns](testing_patterns.md) — role-attach, actingUser Owner helper, partialMock policy proofs, DB::listen fault-injection/race, sync-queue cascade audits, resource/Http::fake/actingAs gotchas, createManyQuietly signature, tokenizer-based "never references X" check, Larastan dead-catch-on-encrypted-cast fix, isolating body-read-once proofs from middleware
- [Frontend checks](frontend_checks.md) — pnpm types:check/lint:check/format:check triad, Prettier-on-Vue gotcha, flex/grid min-w-0 truncate + legend/fieldset gap gotchas, Tooltip-in-Dialog composition, reka-ui Select/Checkbox a11y quirks, parenthesised-vs-bare action-column header convention (sr-only vs visible), stale Wayfinder routes need `--with-form` regen
- [Manual verification recipe](manual_verification_recipe.md) — seed via sail tinker, check via scoped Playwright selectors; standing trap — public/hot + live Vite dev server fakes a "fresh build" (review-07 Finding 8, hid PR #12's withAlpha bug); Destination only accepts POST/PUT; dark-mode screenshot needs a wait after toggling the class; useAppearance only reacts to real updateAppearance() calls, not a raw .dark class toggle — dynamically import the live chunk to drive it headlessly
- [Charting: chart.js + vue3-chartjs](charting_vue3_chartjs.md) — wrapper defects (frozen props snapshot, exports map with no types condition) fixed upstream in 3.0.0, which also watches every prop, so no workaround and no update() call remain here; diagnoses kept because they generalise; null datapoint + spanGaps:false for an honest no-data gap
- [Schema & migrations](schema_migrations.md) — LONGBLOB raw-ALTER migration blocks SQLite entirely (suite is MySQL-only in practice); DB::raw(match) must be inlined for PHPStan literal-string; MySQL JSON key order, information_schema tests, encrypted-cast NULL passthrough, InnoDB auto-drops redundant FK index, DDL-in-RefreshDatabase-test escapes parallel-worker sandbox and leaks rows into unrelated later tests
- [Frontend illustration/animation](frontend_illustration_animation.md) — Tailwind v4 stacked variants + full-tree content scan, Vue scoped-CSS keyframes don't reach inline template style, cropped-screenshot verification for pulse/gradient work
- [Laravel Actions gotchas](laravel_actions_gotchas.md) — AsCommand needs explicit Actions::registerCommands(), Carbon\Carbon* import paths (now() is CarbonImmutable), AsJob-only actions have no ::run(), asJob() beats handle() in JobDecorator (dual entry points), Queue::fake() job-class scoping never matches JobDecorator, afterCommit fires under sync+RefreshDatabase, @phpstan-impure for check-then-recheck races
- [Config sanity guards](config_sanity_guards.md) — guard at the value's resolver seam or at action entry; fail loudly (except clamp-not-throw on a hot request path like ingest); test blank/non-numeric env via putenv+require; guard every config key a formula reads
- [Review rework convention](review_rework_convention.md) — record a Reviewer-finding fix as a "Rework (review-NN finding X)" bullet appended to the owning task's completion notes in docs/tasks/, never a separate doc; docs/status.md stays orchestrator-owned
- [Soft-delete relation pattern](soft_delete_relation_pattern.md) — belongsTo to a SoftDeletes parent resolves null; fetch trashed-inclusive at the call site (established precedent), never widen the non-nullable consumer; check @property-read nullability annotations match reality
- [Infra: deploy target undefined](infra_deploy_target_undefined.md) — no hosting platform decided yet (verified current as of 2026-08-26); bootstrap/app.php trustProxies(at: '*') is an interim placeholder, revisit once a deploy target is chosen
- [Worktree live verification](worktree_live_verification.md) — Sail mounts only the primary checkout; verify a worktree's changes via local composer install + throwaway mysql db + artisan serve, never sail, when another agent owns the primary checkout's branch
- [Laravel Resources gotchas](laravel_resources_gotchas.md) — JsonResource silently array_values()s any all-numeric-keyed nested array unless #[PreserveKeys] is set; verify against a real HTTP response, not just toArray()
