# Technology Stack

> **Status: Populated from repository evidence — pending Owner approval.** This table is a factual capture of what the codebase actually uses (composer/pnpm lockfiles, CI config, project config files) as of 2026-08-03. It is not yet ratified. The Principal Engineer designs within this stack; changes require an Owner-approved ADR. Cells marked "Not yet defined — Owner decision" have no evidence in the repo and must be decided by the Project Owner before they are relied upon.

| Area | Choice | Notes |
|---|---|---|
| Languages | PHP `^8.5` · TypeScript `5.9.3` · Node `>=22` | PHP constraint from `composer.json`, CI runs PHP 8.5. TS pinned in `pnpm-lock.yaml`; Node floor from `package.json` `engines`. |
| Frameworks | Laravel `13.23.0` (constraint `^13.17`) · Vue `3.5.40` · Inertia.js (server `inertia-laravel 3.2.1`, client `@inertiajs/vue3 ^3.6.1`) | SPA-over-Laravel via Inertia. Also: Laravel Fortify `1.37.3` (auth/2FA), `@laravel/passkeys` (WebAuthn), `lorisleiva/laravel-actions ^2.10`, Laravel Wayfinder + Chisel. UI: Tailwind CSS `4.3.3`, Reka UI `2.10.1`, Vite `8.2.0`. |
| Infrastructure | Laravel Sail `1.64.0` (Docker, local dev) | Sail is a dev dependency; `CLAUDE.md` invokes `./vendor/bin/sail`. No `docker-compose.yml` committed at repo root (Sail default not yet published/customized). No orchestration (K8s/ECS) present. |
| Cloud | Not yet defined — Owner decision | No provider config, IaC, or region evidence in the repo. (`.env.example` has empty `AWS_*` placeholders from the Laravel skeleton — not an adopted provider.) |
| CI/CD | GitHub Actions — `.github/workflows/tests.yml` | Triggers: push to `main` + all PRs. Runner `ubuntu-latest`, PHP 8.5, pnpm, Node 22, MySQL 8.0 service container. Runs `composer setup` then `composer ci:check` (ESLint check, Prettier check, `vue-tsc`, then `php artisan test` which also runs Pint `--test` + PHPStan). Action versions pinned to commit SHAs. No deploy job or defined environments. |
| Database | Local/default: SQLite · CI + test suite: MySQL `8.0` · Migrations: Laravel migrations | `DB_CONNECTION=sqlite` in `.env.example`; `config/database.php` default is `sqlite`. CI overrides to MySQL 8.0 service. **Production engine not yet ratified** — Owner decision. Cache/session default to `database` driver in `.env.example`. **The queue now defaults to `redis`** — Laravel Horizon supervises Redis queues only, and on `database` it runs while showing nothing, because it watches a queue the application never writes to. |
| Queue dashboard | Laravel Horizon `^5.48` at `/horizon` | Redis-only by design. Access is HTTP Basic against `HORIZON_USERNAME`/`HORIZON_PASSWORD` rather than a user allow-list, because this project has no superadmin role — operational access is deployment configuration, not a property of an application account. Both the route middleware and Horizon's own `viewHorizon` gate consult the same check, so removing the middleware from `config/horizon.php` cannot open the dashboard. Unset credentials fail closed. Basic auth is base64-encoded, not encrypted — serve over HTTPS. |
| Testing | Backend: PHPUnit `12.5.33` (class-based, `extends TestCase`) + Paratest `7.20.0` (parallel), Faker + Mockery. **Frontend: none.** | Suites `tests/Unit` and `tests/Feature` (`phpunit.xml`). Not Pest — the pest plugin is allow-listed but Pest is not installed and no `tests/Pest.php` exists. **Known gap: no frontend/JS test framework (no Vitest/Jest/Playwright) — Vue components are untested. Owner decision to close.** |
| Static Analysis | PHPStan via Larastan `3.10.0` at **level 7** (`phpstan.neon`) · `vue-tsc` (TS `--noEmit`) · ESLint `9.39.5` (flat config, `typescript-eslint`, `eslint-plugin-vue`) | `composer types:check` = `phpstan analyse`; `pnpm types:check` = `vue-tsc --noEmit`; `pnpm lint:check` = `eslint .`. All run in CI. |
| Formatting | PHP: Laravel Pint `1.30.0`, `laravel` preset (`pint.json`) · JS/TS/Vue: Prettier `3.9.6` + `prettier-plugin-tailwindcss` (targets `resources/`) | `composer lint` = `pint --parallel`; CI enforces `pint --test` and `prettier --check`. |
| Package Managers | Composer (`composer.lock` committed) · pnpm `10.19.0` (`pnpm-lock.yaml` committed) | pnpm pinned via `packageManager` field. **Not npm** — no `package-lock.json`. Both lockfiles committed; lockfile-driven installs in CI (`composer setup`). |
| Security | Secrets via `.env` (`.env.example` template, not committed real values) · Auth: Fortify + passkeys + 2FA · Dependabot (GitHub Actions ecosystem only) | `.github/dependabot.yml` covers **only `github-actions`** — no Composer or pnpm dependency scanning, and no SAST/secret-scanning in CI. **Gap: application-dependency vulnerability scanning is absent — Owner decision.** Ingest guard env vars (`INGEST_MAX_BODY_BYTES`, rate limit) are placeholder defaults per `.env.example` / ADR-006. |
| Performance | Not yet defined — Owner decision | No performance budgets, load-test config, or profiling tooling committed. (Laravel Pail is present for log tailing in dev only; not a perf tool.) |
| Deployment | Not yet defined — Owner decision | No deploy pipeline, Dockerfile, `fly.toml`, Vapor, `Procfile`, or rollback strategy in the repo. CI stops at test/lint. |

> **Optional — semantic code tools:** the development roles use symbol-level code navigation and editing automatically when a matching MCP server is configured in this project. Two supported options — configure either, named exactly `serena` or `jetbrains` in `.mcp.json`:
> - [Serena](https://github.com/oraios/serena) (`uv tool install serena-agent`, then `serena init`) — free, headless; works whenever agents run
> - JetBrains MCP server — richer IDE-grade analysis, but requires the IDE running with this project open
